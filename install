#!/bin/bash

# yay install
if  ! which yay; then
    sudo pacman -S --noconfirm base-devel
    git clone https://aur.archlinux.org/yay.git
    cd yay
    makepkg --noconfirm -si
    cd ../
    rm -rf yay
fi
